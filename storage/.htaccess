# Deny all web access to storage (logs, cache, uploads, sessions).
# These files are never meant to be served publicly.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order Allow,Deny
    Deny from all
</IfModule>
